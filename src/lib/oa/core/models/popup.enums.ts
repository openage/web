export enum PopupTrigger {
  LEFT_CLICK = 'lclick',
  RIGHT_CLICK = 'rclick',
  HOVER = 'hover',
}

export enum PopupPosition {
  ABOVE = 'above',
  BELOW = 'below',
  LEFT = 'left',
  RIGHT = 'right',
  DYNAMIC = 'dynamic'
}

export enum PopupTheme {
  DARK = 'dark',
  LIGHT = 'light'
}

export enum PopupType {
  MENU = 'menu',
  TOOLTIP = 'tooltip',
  DIALOG = 'dialog'
}
